{
  "name": "@openclaw-agent-mesh/gateway",
  "version": "0.9.0",
  "private": true,
  "description": "Controlled Agent Mesh gateway sidecar: policy, runner dispatch boundary, Discord boundary, proactivity, host bindings, and MCP ingress.",
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "jose": "^6.2.8",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
